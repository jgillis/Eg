unsafe: shows how thread-unsafety leads to disaster
doublesafe: shows how resource doubling avoid disaster. Runs at 200% CPU
mutexsafe: shows how to use a mutex to avoid disaster. Runs at 200% CPU on my QUAD machine, but only at 137% CPU on INSPIRON LAPTOP with two processors
